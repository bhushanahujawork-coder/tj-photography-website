# Backend
BACKEND_PORT=8000
BACKEND_HOST=0.0.0.0

# Database (PostgreSQL — configured later)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/tj_gallery

# Storage (local SSD — migration to Cloudflare R2 later)
STORAGE_BACKEND=local
STORAGE_ROOT=./storage

# Cloudflare R2 (configure when migrating)
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=tj-gallery
# R2_PUBLIC_URL=

# Security
SECRET_KEY=change-me-to-a-random-secret-key

# InsightFace (configured later)
# INSIGHTFACE_MODEL_PATH=./models/insightface
